let age = 26
let dogYears = (age + 1) / 7;

console.log(dogYears);

// Challenge Area
// student score 18

let studentScore = 18;

// MaxScore 20
let maxScore = 20;

// percent variable
percentTotal = (studentScore / maxScore) * 100 + '%';

// print
console.log(percentTotal);